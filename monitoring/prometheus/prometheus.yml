global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'backend'
    basic_auth:
      username: 'monitor'
      password: 'monitor'
    static_configs:
      - targets: ['host.docker.internal:5003']
    metrics_path: '/metrics'
    scheme: 'http'
